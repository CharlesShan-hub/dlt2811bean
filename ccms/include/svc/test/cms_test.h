/*
 * Test (Service Code 0xA1)
 *
 * This service has no service-specific fields.
 * Both request and response carry only reqId (Int16U).
 * FL = 0, no additional fields — handled entirely by the base service layer.
 *
 * No typedefs, no encode/decode declarations needed.
 */
