export default {
  title: '测试 test (8.14)',
  desc: '测试连接（Ping/Pong，需已关联）',
  asn1: `Test-RequestPDU ::= NULL  // 无 ASDU，仅 APCH 头（FL = 0） — 8.14`,
  note: '应用层 Test 服务（8.14）：发送空测试报文（ASN.1 定义 FL = 0，无字段），服务器响应以确认链路与关联正常。',
  doc: `## 协议原文

测试服务用于通信连接的双方测试链路是否正常、对方是否能正确响应请求。当接收到 Test 帧时，应立刻返回一个 Test 帧。测试服务仅有 APCH 头，不含 ASDU 部分，帧长度 \`FL=0\`。

## 设计要点

Test 服务身兼两职：**连通性测试** 与 **心跳保活**。

### 连通性测试

1. **验证连接是否正确**：用于测试目标 IP 和端口是否可达、通信链路是否正常。发送 Test 帧后，对端应立即返回一个 Test 帧（Ping/Pong 模式），一次往返即可确认链路可用。
2. **轻量无状态**：报文仅含 APCH 头、无 ASDU 数据（\`FL=0\`），开销极小，可随时发起。

### 心跳机制

1. **应用层心跳**：关联建立后应定时进行通信状态检测。链路长时间空闲时，客户和服务器均可主动发出 Test 报文，探测对端程序是否仍处于工作状态。发送周期宜选择 1min~5min，收到任何有效报文后计时器重新计数。
2. **TCP KeepAlive 兜底**：同时启用 TCP 的 KeepAlive 机制作为传输层检测（空闲 30s、间隔 5s、4 次），网络接口或线缆损坏时最长 50s 内即可发现网络故障，弥补应用层心跳的探测间隙。`,
  params: [],
}
