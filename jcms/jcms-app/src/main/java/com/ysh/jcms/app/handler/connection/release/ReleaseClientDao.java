package com.ysh.jcms.app.handler.connection.release;

/**
 * Pure data object for Release-Request parameters.
 */
public class ReleaseClientDao {
    /** No input fields needed for Release — it just releases the current association. */
}
