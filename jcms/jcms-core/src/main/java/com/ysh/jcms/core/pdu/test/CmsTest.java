package com.ysh.jcms.core.pdu.test;

/**
 * Test (Service Code 0xA1)
 *
 * This service has no service-specific fields. Both request and response carry
 * only reqId (Int16U). FL = 0, no additional fields — handled entirely by the
 * base infrastructure.
 *
 * No Java class body needed; only the package marker for JNA mapping.
 */
