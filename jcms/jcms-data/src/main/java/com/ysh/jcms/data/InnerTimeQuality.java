// Auto-generated by csasn1 v0.1.0 at 2026-07-24 09:04 UTC: TimeQuality
//

package com.ysh.jcms.data;

import com.fasterxml.jackson.annotation.*;
import com.fasterxml.jackson.databind.*;
import lombok.Data;

/**
 * <pre>{@code
 * TimeQuality ::= BIT STRING {
 *     leap-second-known            (0),
 *     clock-failure                (1),
 *     clock-not-synchronized       (2)
 * } (SIZE(8))
 * }</pre>
 */
@Data
@lombok.experimental.Accessors(chain = true, fluent = true)
public class InnerTimeQuality extends InnerBase {
    private static final ObjectMapper MAPPER = InnerBase.createMapper();
    public int value;
    public InnerTimeQuality() {}
    public InnerTimeQuality(int value) { this.value = value; }
    @JsonValue
    public String toJsonValue() { return InnerBase.bitStringHex(this.value, 8); }
    @JsonCreator
    public InnerTimeQuality(String hex) { this.value = InnerBase.parseBitStringHex(hex, 8); }
    public byte[] encode() {
        try {
            return InnerNative.encode("TimeQuality", DEFAULT_ENCODING, MAPPER.writeValueAsString(InnerBase.bitStringHex(this.value, 8)));
        } catch (Exception e) {
            throw new RuntimeException(e);
        }
    }
    public byte[] encodeTest() {
        try {
            return InnerNative.encode("TimeQuality", DEFAULT_ENCODING, MAPPER.writeValueAsString(InnerBase.bitStringHex(this.value, 8)));
        } catch (Exception e) {
            throw new RuntimeException(e);
        }
    }
    public static InnerTimeQuality decode(byte[] data) {
        try {
            String json = InnerNative.decode("TimeQuality", DEFAULT_ENCODING, data);
            InnerTimeQuality r = new InnerTimeQuality();
            r.value = InnerBase.parseBitStringHex(MAPPER.readTree(json).get("value").asText(), 8);
            return r;
        } catch (Exception e) {
            throw new RuntimeException(e);
        }
    }
}
